using UnityEngine;

public class logscript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("PHAM NHU THANG - NOP BAI");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("PHAM NHU THANG - NOP BAI");
    }
}
